**bambino > quirks > models > p1**

# Module: quirks::models::p1

## Contents

**Structs**

- [`P1Quirks`](#p1quirks) - Quirks shared by the P1P and P1S CoreXY platforms.

**Constants**

- [`P1_BED_TEMP_MAX`](#p1_bed_temp_max) - Bed temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.
- [`P1_NOZZLE_TEMP_MAX`](#p1_nozzle_temp_max) - Nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.
- [`P1_Z_MAX`](#p1_z_max) - Build volume Z depth (mm) shared by P1P and P1S, per `MODEL_MATRIX.csv`'s Build Volume row.

---

## bambino::quirks::models::p1::P1Quirks

*Struct*

Quirks shared by the P1P and P1S CoreXY platforms.

**Unit Struct**

**Trait Implementations:**

- **ModelQuirks**
  - `fn uses_plaintext_ftps_data_channel(self: &Self) -> bool`
  - `fn enforce_ftps_tls_1_2(self: &Self) -> bool`
  - `fn is_door_open(self: &Self, _telemetry: &PrinterTelemetry) -> bool`
  - `fn has_door_sensor(self: &Self) -> bool`
  - `fn camera_protocol(self: &Self) -> CameraProtocol`
  - `fn ignores_chamber_temperature(self: &Self) -> bool`
  - `fn has_stg_cur_idle_bug(self: &Self) -> bool`
  - `fn has_active_chamber_heater(self: &Self) -> bool`
  - `fn physical_nozzle_count(self: &Self) -> u8`
  - `fn supports_nozzle_offset_calibration(self: &Self) -> bool`
  - `fn is_bed_on_z(self: &Self) -> bool`
  - `fn z_max(self: &Self) -> f32`
  - `fn nozzle_temp_max(self: &Self) -> u16`
  - `fn bed_temp_max(self: &Self, _mains_220v: Option<bool>) -> u16`



## bambino::quirks::models::p1::P1_BED_TEMP_MAX

*Constant*: `u16`

Bed temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.



## bambino::quirks::models::p1::P1_NOZZLE_TEMP_MAX

*Constant*: `u16`

Nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.



## bambino::quirks::models::p1::P1_Z_MAX

*Constant*: `f32`

Build volume Z depth (mm) shared by P1P and P1S, per `MODEL_MATRIX.csv`'s Build Volume row.



