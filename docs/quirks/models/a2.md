**bambino > quirks > models > a2**

# Module: quirks::models::a2

## Contents

**Structs**

- [`A2LQuirks`](#a2lquirks) - Quirks for the A2L large-format open-frame bed-slinger.

**Constants**

- [`A2L_BED_TEMP_MAX`](#a2l_bed_temp_max) - Bed temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.
- [`A2L_NOZZLE_TEMP_MAX`](#a2l_nozzle_temp_max) - Nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.
- [`A2L_Z_MAX`](#a2l_z_max) - A2L build volume Z depth (mm), per `MODEL_MATRIX.csv`'s Build Volume row (330×320×325mm).

---

## bambino::quirks::models::a2::A2LQuirks

*Struct*

Quirks for the A2L large-format open-frame bed-slinger.

**Unit Struct**

**Trait Implementations:**

- **ModelQuirks**
  - `fn uses_plaintext_ftps_data_channel(self: &Self) -> bool`
  - `fn enforce_ftps_tls_1_2(self: &Self) -> bool`
  - `fn is_door_open(self: &Self, _telemetry: &PrinterTelemetry) -> bool`
  - `fn has_door_sensor(self: &Self) -> bool`
  - `fn camera_protocol(self: &Self) -> CameraProtocol`
  - `fn ignores_chamber_temperature(self: &Self) -> bool`
  - `fn has_stg_cur_idle_bug(self: &Self) -> bool`
  - `fn has_active_chamber_heater(self: &Self) -> bool`
  - `fn physical_nozzle_count(self: &Self) -> u8`
  - `fn supports_nozzle_offset_calibration(self: &Self) -> bool`
  - `fn is_bed_on_z(self: &Self) -> bool`
  - `fn z_max(self: &Self) -> f32`
  - `fn nozzle_temp_max(self: &Self) -> u16`
  - `fn bed_temp_max(self: &Self, _mains_220v: Option<bool>) -> u16`
  - `fn supports_prompt_sound(self: &Self) -> bool`
  - `fn supports_auxiliary_left_fan(self: &Self) -> bool`



## bambino::quirks::models::a2::A2L_BED_TEMP_MAX

*Constant*: `u16`

Bed temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.



## bambino::quirks::models::a2::A2L_NOZZLE_TEMP_MAX

*Constant*: `u16`

Nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.



## bambino::quirks::models::a2::A2L_Z_MAX

*Constant*: `f32`

A2L build volume Z depth (mm), per `MODEL_MATRIX.csv`'s Build Volume row (330×320×325mm).



