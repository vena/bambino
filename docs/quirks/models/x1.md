**bambino > quirks > models > x1**

# Module: quirks::models::x1

## Contents

**Structs**

- [`X1CQuirks`](#x1cquirks) - Quirks for the X1C — no active chamber heater, voltage-dependent bed ceiling (see `x1c_bed_temp_max`).
- [`X1EQuirks`](#x1equirks) - Quirks for the X1E — active chamber heater, higher nozzle ceiling than X1C.

**Constants**

- [`X1C_BED_TEMP_MAX_110V`](#x1c_bed_temp_max_110v) - Bed temperature ceiling on a 110V-region unit.
- [`X1C_BED_TEMP_MAX_220V`](#x1c_bed_temp_max_220v) - Bed temperature ceiling on a 220V-region unit — confirmed, per the official spec sheet, non-obviously *lower* than the 110V ceiling.
- [`X1C_NOZZLE_TEMP_MAX`](#x1c_nozzle_temp_max) - X1C nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.
- [`X1E_BED_TEMP_MAX`](#x1e_bed_temp_max) - X1E bed temperature ceiling (°C) — flat, not voltage-dependent (see `x1e_bed_temp_max`), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.
- [`X1E_CHAMBER_TEMP_MAX`](#x1e_chamber_temp_max) - X1E chamber temperature ceiling (°C) — X1E has an active chamber heater, X1C does not, per `MODEL_MATRIX.csv`'s Max Chamber Temperature row.
- [`X1E_NOZZLE_TEMP_MAX`](#x1e_nozzle_temp_max) - X1E nozzle temperature ceiling (°C) — higher than X1C's, per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.
- [`X1_Z_MAX`](#x1_z_max) - Build volume Z depth (mm) shared by X1C and X1E, per `MODEL_MATRIX.csv`'s Build Volume row.

---

## bambino::quirks::models::x1::X1CQuirks

*Struct*

Quirks for the X1C — no active chamber heater, voltage-dependent bed ceiling (see `x1c_bed_temp_max`).

**Unit Struct**

**Trait Implementations:**

- **ModelQuirks**
  - `fn uses_plaintext_ftps_data_channel(self: &Self) -> bool`
  - `fn enforce_ftps_tls_1_2(self: &Self) -> bool`
  - `fn is_door_open(self: &Self, telemetry: &PrinterTelemetry) -> bool`
  - `fn has_door_sensor(self: &Self) -> bool`
  - `fn camera_protocol(self: &Self) -> CameraProtocol`
  - `fn ignores_chamber_temperature(self: &Self) -> bool`
  - `fn has_stg_cur_idle_bug(self: &Self) -> bool`
  - `fn has_active_chamber_heater(self: &Self) -> bool`
  - `fn physical_nozzle_count(self: &Self) -> u8`
  - `fn supports_nozzle_offset_calibration(self: &Self) -> bool`
  - `fn is_bed_on_z(self: &Self) -> bool`
  - `fn z_max(self: &Self) -> f32`
  - `fn nozzle_temp_max(self: &Self) -> u16`
  - `fn bed_temp_max(self: &Self, mains_220v: Option<bool>) -> u16`
  - `fn chamber_temp_max(self: &Self) -> u16`



## bambino::quirks::models::x1::X1C_BED_TEMP_MAX_110V

*Constant*: `u16`

Bed temperature ceiling on a 110V-region unit.



## bambino::quirks::models::x1::X1C_BED_TEMP_MAX_220V

*Constant*: `u16`

Bed temperature ceiling on a 220V-region unit — confirmed, per the official spec sheet, non-obviously *lower* than the 110V ceiling.
Also the conservative default when the mains region is unknown (no `home_flag` telemetry
received yet).



## bambino::quirks::models::x1::X1C_NOZZLE_TEMP_MAX

*Constant*: `u16`

X1C nozzle temperature ceiling (°C), per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.



## bambino::quirks::models::x1::X1EQuirks

*Struct*

Quirks for the X1E — active chamber heater, higher nozzle ceiling than X1C.

**Unit Struct**

**Trait Implementations:**

- **ModelQuirks**
  - `fn uses_plaintext_ftps_data_channel(self: &Self) -> bool`
  - `fn enforce_ftps_tls_1_2(self: &Self) -> bool`
  - `fn is_door_open(self: &Self, telemetry: &PrinterTelemetry) -> bool`
  - `fn has_door_sensor(self: &Self) -> bool`
  - `fn camera_protocol(self: &Self) -> CameraProtocol`
  - `fn ignores_chamber_temperature(self: &Self) -> bool`
  - `fn has_stg_cur_idle_bug(self: &Self) -> bool`
  - `fn has_active_chamber_heater(self: &Self) -> bool`
  - `fn physical_nozzle_count(self: &Self) -> u8`
  - `fn supports_nozzle_offset_calibration(self: &Self) -> bool`
  - `fn is_bed_on_z(self: &Self) -> bool`
  - `fn z_max(self: &Self) -> f32`
  - `fn nozzle_temp_max(self: &Self) -> u16`
  - `fn bed_temp_max(self: &Self, mains_220v: Option<bool>) -> u16`
  - `fn chamber_temp_max(self: &Self) -> u16`



## bambino::quirks::models::x1::X1E_BED_TEMP_MAX

*Constant*: `u16`

X1E bed temperature ceiling (°C) — flat, not voltage-dependent (see `x1e_bed_temp_max`), per `MODEL_MATRIX.csv`'s Max Build Plate Temperature row.



## bambino::quirks::models::x1::X1E_CHAMBER_TEMP_MAX

*Constant*: `u16`

X1E chamber temperature ceiling (°C) — X1E has an active chamber heater, X1C does not, per `MODEL_MATRIX.csv`'s Max Chamber Temperature row.



## bambino::quirks::models::x1::X1E_NOZZLE_TEMP_MAX

*Constant*: `u16`

X1E nozzle temperature ceiling (°C) — higher than X1C's, per `MODEL_MATRIX.csv`'s Max Hot End Temperature row.



## bambino::quirks::models::x1::X1_Z_MAX

*Constant*: `f32`

Build volume Z depth (mm) shared by X1C and X1E, per `MODEL_MATRIX.csv`'s Build Volume row.



