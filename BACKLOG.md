# bambino — Findings Backlog

Data only: one row per known bug/gap, `Open`/`Fixed`/`Wontfix`. Doesn't replace REVIEW/PLAN files (full investigative record, linked per row) or hold its own rules — and stands in for a real issue tracker until this repo has a GitHub remote, migrate to Issues then rather than extending this further.

**Rules, severity definitions, release bar, and next-BUG-ID logic live in the `backlog` skill, not here** — invoke it before adding or updating an entry. Keeping the rules out of this file is deliberate: a passive preamble nobody re-reads before editing is exactly how `CLAUDE.md` got out of hand.

---

## Open

| ID | Sev | Module | Title | Found | Detail |
|---|---|---|---|---|---|
| BUG-170 | Sev3 | tests/client_test.rs | Pervasive copy-pasted 3-statement MQTT connection boilerplate (~60 sites, ~3300-line file) | 2026-07-16 | See `RUST-REVIEW.md` unit 7 (client-tests). `duplex`→`BambuMqttClient::connect`→`PrinterClient::from_mqtt` repeated per test; extract a `tests/common` helper. |
| BUG-171 | Sev3 | ftps/client.rs | Poisoning-check-then-return pattern duplicated verbatim 34 times across 13 methods instead of a shared helper | 2026-07-16 | See `RUST-REVIEW.md` unit 9 (ftps-client). Root-cause context on BUG-004's shape (missed poisoning was a copy-paste miss); add `write_command_poisoning`/`read_response_poisoning` helpers. |
| BUG-172 | Sev3 | ftps/client.rs | `upload_file`'s manual offset/chunk_size loop should use `.chunks()` iterator combinator | 2026-07-16 | See `RUST-REVIEW.md` unit 9 (ftps-client). Lines 676-712; `data.chunks(FTPS_UPLOAD_CHUNK_SIZE)` produces identical wire writes, not gated by the wire-framing-hardware-verification rule. |
| BUG-173 | Sev3 | ftps/parser.rs | Repeated identical 4-line token-skip block copy-pasted 3x in `parse_unix_listing` | 2026-07-16 | See `RUST-REVIEW.md` unit 10 (ftps-protocol). Lines 141-153; collapse to a `for _ in 0..3` loop. |
| BUG-174 | Sev3 | ftps/protocol.rs | `parse_pasv_port` uses 4 manual `let _ = parts.next();` instead of `.skip(4)` | 2026-07-16 | See `RUST-REVIEW.md` unit 10 (ftps-protocol). Lines 327-330. |
| BUG-179 | Sev3 | quirks/models/h2.rs | Doc comment wraps mid-sentence, violating the project's mandatory doc-comment rule; sole outlier among 8 sibling files | 2026-07-16 | See `RUST-REVIEW.md` unit 16 (quirks-models). Lines 23-24 (`H2S_X_MAX`). |
| BUG-181 | Sev3 | bin/bambino-cli/** | CLI reuses library `Error::ProtocolViolation` as a catch-all for CLI-local failures (bad args, file I/O, terminal init, stdin read) unrelated to wire protocol violations | 2026-07-16 | See `RUST-REVIEW.md` units 20-21 (cli-core, cli-commands). 12+ sites across connection.rs, verify_tls.rs, inspect_cert.rs, camera.rs, control.rs, storage.rs, monitor/mod.rs, probe.rs; introduce a CLI-local error type instead. |
| BUG-182 | Sev3 | bin/bambino-cli/storage.rs | `fs::metadata` error discarded, reports misleading "does not exist" for any failure (permission-denied, symlink loop, etc.) | 2026-07-16 | See `RUST-REVIEW.md` unit 21 (cli-commands). Line 123; include the real `e` in the message instead of `map_err(|_| ...)`. |
| BUG-183 | Sev3 | bin/bambino-cli/monitor/dashboard.rs | Needless full clone of the `_device` JSON subtree on every dashboard redraw in the live monitor loop | 2026-07-16 | See `RUST-REVIEW.md` unit 21 (cli-commands). Lines 270-274; `DeviceTelemetry::deserialize(v)` works directly on `&Value`, no `.clone()` needed. |
| BUG-187 | Sev3 | client/camera.rs, client/storage.rs | `self.camera`/`self.ftps` `.unwrap()` should be `.expect("...")` naming the `ensure_camera()`/`ensure_ftps()` invariant that makes it safe | 2026-07-16 | See `RUST-REVIEW.md` unit 6 (client-commands). camera.rs:68,79-81; storage.rs:65. |
| BUG-188 | Sev3 | client/ams.rs | Duplicated `ams_valid` address-space validation block in `change_filament()`/`select_k_profile()` | 2026-07-16 | See `RUST-REVIEW.md` unit 6 (client-commands). Lines 82-85, 247-250; extract a shared `is_valid_ams_id()` helper. |
| BUG-189 | Sev3 | ftps/parser.rs | `parse_unix_listing` takes 4 consecutive same-typed (`u8`) positional params — transposition footgun with no compiler catch | 2026-07-16 | See `RUST-REVIEW.md` unit 10 (ftps-protocol). Line 398; bundle into a small `CurrentDateTime` struct. |
| BUG-192 | Sev3 | types/telemetry/device.rs | `active_extruder_index()`/`extruder_count()` use `.map().unwrap_or()` instead of `.map_or()` | 2026-07-16 | See `RUST-REVIEW.md` unit 19 (types-device). Lines 378-386. |
| BUG-194 | Sev3 | bin/bambino-cli/monitor/dashboard.rs | ~40 repeated `writeln!(...).unwrap_or(())`/`write!(...).unwrap_or(())` should be a shared macro/helper | 2026-07-16 | See `RUST-REVIEW.md` unit 21 (cli-commands). Throughout `render_*` helpers, e.g. lines 121-155, 218-260. |
| BUG-195 | Sev3 | crate-wide | No `#[must_use]` anywhere in the crate despite many pure-return builder/getter methods — independently noted by 3 separate review units | 2026-07-16 | See `RUST-REVIEW.md` units 5, 17, 19 (client-core, types-core, types-device). Start with `PrinterClient`'s type-parameter-changing builders (`with_timer`/`with_ftps`/`with_camera`) where a dropped return silently loses a whole connector config. |
| BUG-196 | Sev3 | ftps/client.rs | `connect`/`connect_control_stream`/`from_control_stream` suppress `clippy::too_many_arguments` (8-9 positional args, several same-typed `&str` adjacent) instead of a config struct | 2026-07-16 | See `RUST-REVIEW.md` unit 9 (ftps-client). Lines 152-165, 186-198, 291-303; needs a design decision on the replacement shape before implementing (interacts with `PrinterClient::ensure_ftps`). |
| BUG-198 | Sev3 | bin/bambino-cli/probe.rs | `run()` is a single ~235-line function mixing 5 distinct responsibilities (confirm, connect, pushall capture, test-dispatch loop, report write) | 2026-07-16 | See `RUST-REVIEW.md` unit 21 (cli-commands). Lines 365-601; extract `confirm_or_abort()`/`run_pushall_capture()`/`run_test_loop()`. |

## Fixed

| ID | Sev | Module | Title | Found | Closed | Detail |
|---|---|---|---|---|---|---|
| BUG-166 | Sev3 | ams/mapping.rs, ams/parser.rs | Pure return-value-only functions (`validate_external_spool_safety` and ~8 siblings) missing `#[must_use]` | 2026-07-16 | 2026-07-16 | Added `#[must_use]` to 8 pure fns in mapping.rs + 3 in parser.rs (all non-mutating value/Option/bool returns; `clean_stale_tray_data`'s in-place mutator excluded). |
| BUG-167 | Sev3 | diagnostics/kprofile.rs | Setting-ID validation error-construction boilerplate duplicated verbatim in two constructors | 2026-07-16 | 2026-07-16 | Extracted `ensure_valid_setting_id(&str) -> Result<(), Error>`; both constructors now use `?`. |
| BUG-185 | Sev3 | ams/mapping.rs, diagnostics/kprofile.rs | `validate_*()` bool-returning fns named as verbs, not predicates (C-QUESTION-BOOL) | 2026-07-16 | 2026-07-16 | Renamed to `is_*_valid` uniformly in both files: `is_external_spool_safety_valid`(_flat), `is_ams_pool_composition_valid`, `is_setting_id_valid`. |
| BUG-165 | Sev3 | models.rs | `BambuModel` redundantly repeats the crate's own subject, same pattern as the prior `BambuError`→`Error` fix | 2026-07-16 | 2026-07-16 | Renamed `BambuModel`→`PrinterModel` (mirrors `PrinterClient`), re-exported as `bambino::PrinterModel`; updated ~200 call sites across src/ and tests/. |
| BUG-168 | Sev3 | camera/rtsps.rs | `RtpTimestampCorrector::init` breaks crate-wide `new` constructor naming convention | 2026-07-16 | 2026-07-16 | Renamed `init`→`new`; sole call sites were rtsps.rs's own tests. |
| BUG-169 | Sev3 | client/connect.rs | `mqtt_connected`/`ftps_connected`/`camera_connected` missing `is_` prefix used by every sibling bool predicate | 2026-07-16 | 2026-07-16 | Renamed to `is_mqtt_connected`/`is_ftps_connected`/`is_camera_connected`; updated all call sites in src/ and tests/. |
| BUG-175 | Sev3 | io/esp_idf.rs | `EspTlsStream` breaks the module's own `EspIdf`-prefix naming convention | 2026-07-16 | 2026-07-16 | Renamed `EspTlsStream`→`EspIdfTlsStream`. |
| BUG-176 | Sev3 | quirks/mod.rs | `enforce_ftps_tls_1_2` breaks the trait's third-person-singular boolean-predicate naming convention | 2026-07-16 | 2026-07-16 | Renamed `enforce_ftps_tls_1_2`→`enforces_ftps_tls_1_2`; updated ~9 model impls + call sites. |
| BUG-177 | Sev3 | quirks/mod.rs | `door_sensor_field_present` lacks the `is_`/`has_` prefix used by its neighboring door-sensor predicates | 2026-07-16 | 2026-07-16 | Renamed `door_sensor_field_present`→`has_door_sensor_field` (dropped redundant "present"). |
| BUG-178 | Sev3 | types/telemetry/ams.rs | `AmsTray::get_state()` uses the disallowed `get_` prefix on a plain accessor | 2026-07-16 | 2026-07-16 | Renamed `AmsTray::get_state()`→`state()`; updated client/telemetry.rs call sites. |
| BUG-180 | Sev3 | mqtt/client/mod.rs | `get_in_flight_count()` uses the disallowed `get_` prefix on a plain getter | 2026-07-16 | 2026-07-16 | Renamed `get_in_flight_count()`→`in_flight_count()`. |
| BUG-184 | Sev3 | error.rs | `Error::NetworkError`/`Error::SerializationError` redundantly suffix "Error" on a variant of an enum already named `Error`, inconsistent with sibling variants | 2026-07-16 | 2026-07-16 | Renamed `Error::NetworkError`→`Error::Network`, `Error::SerializationError`→`Error::Serialization`; updated call sites crate-wide. |
| BUG-186 | Sev3 | client/telemetry.rs | `door_open()` lacks the `is_` prefix used by its sibling door-sensor decoders | 2026-07-16 | 2026-07-16 | Renamed `door_open()`→`is_door_open()`. |
| BUG-190 | Sev3 | io/tokio.rs | `to_socket_error` should be `impl From<std::io::Error> for SocketError`, not a free fn called via `.map_err()` at 6 sites | 2026-07-16 | 2026-07-16 | Replaced free fn with `impl From<std::io::Error> for SocketError`; call sites simplified to `?`/`SocketError::from`. |
| BUG-191 | Sev3 | quirks/mod.rs | `auxiliary_fan_uses_percentage` is subject-first, inconsistent with the trait's prevailing verb-first predicate naming | 2026-07-16 | 2026-07-16 | Renamed `auxiliary_fan_uses_percentage`→`reports_auxiliary_fan_percentage` (verb-first, matches sibling `uses_plaintext_ftps_data_channel`). |
| BUG-193 | Sev3 | bin/bambino-cli/main.rs | `enum Command` collides in name with `clap::Command`, the builder type from the same dependency | 2026-07-16 | 2026-07-16 | Renamed `enum Command`→`Commands`, the standard clap-derive convention. |
| BUG-197 | Sev3 | mqtt/client/mod.rs | `BambuMqttClient` redundantly repeats the crate's own subject, same class as the `BambuError`→`Error` fix and `BambuModel` (BUG-165) | 2026-07-16 | 2026-07-16 | Renamed `BambuMqttClient`→`MqttClient`; confirmed no naming collision; updated call sites across src/ and tests/. |
| BUG-163 | Sev3 | client/motion.rs, quirks/mod.rs, quirks/models/*.rs | X/Y relative moves had no distance cap, unlike Z's `z_max` | 2026-07-16 | 2026-07-16 | Added `x_max()`/`y_max()` quirks trait methods (per-model overrides from `MODEL_MATRIX.csv`'s Build Volume row, conservative min across nozzle profiles where models have more than one) and `format_xy_move_gcode`/`relative_xy_move_gcode`, mirroring `z_max`/`format_z_move_gcode`. `move_relative`'s X/Y branch now rejects an out-of-range distance the same way Z's branch already does. Same limitation as Z's cap (single-command distance bound, not position-aware crash prevention — no absolute axis telemetry exists to clamp from). |
| BUG-164 | Sev3 | ftps/client.rs, ftps/protocol.rs | `upload_file`'s data-write loop had no stall-timeout protection, unlike everything around it | 2026-07-16 | 2026-07-16 | Added `FTPS_WRITE_TIMEOUT_SECS` and raced each chunk's `write_all`/final `flush()` against it via `crate::io::race`, mirroring BUG-159's `write_frame_with_timer` for MQTT. A stalled write now surfaces `NetworkError(TimedOut)` instead of hanging forever. |
| BUG-158 | Sev2 | types/telemetry/device.rs | `NozzleCollection`/`ExtruderCollection`/`AirductCollection`'s `Vec<T>` fields couldn't distinguish absent-key from explicit-empty-array on partial pushes, same shape as BUG-102 | 2026-07-16 | 2026-07-16 | src/types/telemetry/device.rs — `info`/`parts`/`mode_list` are now `Option<Vec<_>>` (`None`=absent/preserve, `Some(vec![])`=present-empty/clear), mirroring BUG-102's `AmsTray` fix. Confirmed via BambuStudio's `json_diff::restore_objects` (`src/slic3r/Utils/json_diff.cpp`, the same wire-verified merge engine BUG-095/096/097/098/101 already cited) — a present array differing from the cached value (including `[]` replacing non-empty) is authoritative; only an absent key preserves. pybambu's `airduct.modeList` handling independently agrees; bambuddy's H2C nozzle-rack "skip empty" fix was traced to real hardware (issue #300) but is UI-flicker smoothing, not a wire-semantics claim, so doesn't contradict this. BUG-094 (Fixed) is not reopened — its absence-handling premise was correct, just incomplete. |
| BUG-144 | Sev3 | ams/mapping.rs | Doc comments say AMS-HT `ams_id` is "128+"; code enforces 128-135 only | 2026-07-16 | 2026-07-16 | src/ams/mapping.rs:28,133 — doc comments now say "128-135", matching `AMS_HT_ID_MIN`/`AMS_HT_ID_MAX` |
| BUG-145 | Sev3 | bin/bambino-cli/probe.rs | `pushall` step aborts the entire probe run via `?`, discarding all output | 2026-07-16 | 2026-07-16 | src/bin/bambino-cli/probe.rs:430-452 — `request_pushall()`/`capture_pushall()` errors now caught and reported per-branch instead of propagated via `?`, matching BUG-017's capture_responses() pattern |
| BUG-146 | Sev3 | client/connect.rs | `disconnect_mqtt()`'s doc claims a lazy-redial fallback that's permanently broken for `from_mqtt()`-built clients | 2026-07-16 | 2026-07-16 | src/client/connect.rs:147-150 — doc now states the lazy-dial fallback only recovers a `connect()`-built client, never a `from_mqtt()` one (`dummy.rs`/`mod.rs` already documented this correctly) |
| BUG-147 | Sev3 | tests/client_test.rs | Zero test coverage for `PrinterClient` camera trio's "not configured" branch | 2026-07-16 | 2026-07-16 | tests/client_test.rs — added test_camera_trio_unconfigured_error; RTSPS fail-fast, attach/disconnect were already covered in tests/camera_test.rs |
| BUG-148 | Sev3 | tests/client_test.rs | `FanTarget::ChamberExhaust` never exercised through `set_fan_speed` | 2026-07-16 | 2026-07-16 | tests/client_test.rs — added test_chamber_exhaust_fan_success_and_model_mismatch (H2D success, P1S ModelMismatch) |
| BUG-149 | Sev3 | ftps/parser.rs | `parse_unix_listing`'s explicit-`YYYY` year branch has no validation, unlike day/hour/minute (BUG-061) | 2026-07-16 | 2026-07-16 | src/ftps/parser.rs:246-252 — parse failure now rejects the line instead of defaulting to current_year; parsed value range-checked to 2000..=9999 |
| BUG-150 | Sev3 | ftps/client.rs | `list_directory` rejects `426` unconditionally, unlike `upload_file`/`download_file` (BUG-030) | 2026-07-16 | 2026-07-16 | src/ftps/client.rs:521-527 — now accepts `FTP_TRANSFER_ABORTED` alongside `FTP_TRANSFER_COMPLETE`, matching upload/download's symmetric handling |
| BUG-151 | Sev3 | types/telemetry/report.rs | Doc comments on `nozzle_target_temper`/`bed_temper` wrongly instruct composite-unpack | 2026-07-16 | 2026-07-16 | src/types/telemetry/report.rs:108-120 — doc comments now state these fields are never composite-packed, matching this dir's CLAUDE.md |
| BUG-152 | Sev3 | bin/bambino-cli/probe.rs | Per-test capture window can silently overshoot up to 10x | 2026-07-16 | 2026-07-16 | src/bin/bambino-cli/probe.rs:189-196,342-350 — each `poll_telemetry()` call is now raced against the remaining per-window deadline via `tokio::time::timeout`, capping overshoot to a single poll |
| BUG-153 | Sev3 | bin/bambino-cli/table.rs | Column width/padding use byte/char count, not display width | 2026-07-16 | 2026-07-16 | src/bin/bambino-cli/table.rs — switched to `unicode-width` crate (new cli-gated dep) for width computation and padding |
| BUG-154 | Sev3 | camera/binary.rs | `read_next_frame`'s doc comment claims buffer reuse that doesn't happen | 2026-07-16 | 2026-07-16 | src/camera/binary.rs:364 — doc now states the buffer is wholesale-replaced (`*frame_buf = payload`), not refilled |
| BUG-155 | Sev3 | discovery/parser.rs | `parse_location` accepts an empty host string, unlike its own port validation (BUG-084) | 2026-07-16 | 2026-07-16 | src/discovery/parser.rs:66-68 — empty host now rejected (`None`), mirroring BUG-084's port validation |
| BUG-156 | Sev3 | discovery/parser.rs | `parse_ssdp_payload` accepts an empty USN serial, unlike `DevModel`'s empty-string handling (BUG-047) | 2026-07-16 | 2026-07-16 | src/discovery/parser.rs:208-210 — empty `raw.usn` now rejected (`None`), mirroring BUG-047 |
| BUG-157 | Sev3 | io/tokio/tests.rs | `test_build_verified_client_config_bad_key_returns_error` never calls `build_verified_client_config` | 2026-07-16 | 2026-07-16 | src/io/tokio/tests.rs:394-410 — now builds a well-typed-but-garbage PKCS#8 key and asserts `build_verified_client_config` itself errors |
| BUG-159 | Sev3 | mqtt/client/mod.rs | `write_frame` has no stall-timeout protection, unlike the read path | 2026-07-16 | 2026-07-16 | src/mqtt/client/mod.rs — added `write_frame_with_timer`/`MQTT_WRITE_TIMEOUT_SECS`; `publish_command_with_timer`/`send_ping_with_timer` thread `PrinterClient`'s `self.timer` through, mirroring `poll_telemetry_with_timer` |
| BUG-160 | Sev3 | quirks/mod.rs | `DEFAULT_Z_MAX_MM`'s doc comment names an unused default and gets 2 named models wrong | 2026-07-16 | 2026-07-16 | src/quirks/mod.rs:290-295 — doc now states every model overrides `z_max()` (constant unreachable) and drops the wrong A1 Mini/H2S values |
| BUG-161 | Sev3 | camera/binary.rs, camera/mod.rs | No doc warning against fast port-6000 redial (printer-side single-connection socket) | 2026-07-16 | 2026-07-16 | src/camera/binary.rs:121-127, src/camera/mod.rs:20-24 — added doc warnings on `BambuBinaryCameraStream` and `CAMERA_PORT_BINARY_JPEG` about the ~20 min orphaned-socket stall on fast redial |
| BUG-127 | Sev2 | ams/mapping.rs | `flat_channel_id_for_entry` doesn't bound-check `slot_id`, unlike its inverse `flat_channel_id` | 2026-07-15 | 2026-07-16 | src/ams/mapping.rs:147 — added the same `slot_id < AMS_SLOTS_PER_UNIT` bound check `flat_channel_id()` already had, falling back to `-1` on an out-of-range `slot_id` |
| BUG-128 | Sev2 | ams/mapping.rs | `validate_external_spool_safety` misclassifies out-of-range `AmsMapping2Entry` as physical AMS | 2026-07-15 | 2026-07-16 | src/ams/mapping.rs:239-259 — now requires a validly-ranged standard or AMS-HT entry (`is_valid_physical`) before counting it as physical, instead of any non-unmapped/non-external entry |
| BUG-129 | Sev2 | bin/bambino-cli/monitor/mod.rs | `dump --follow`'s ping/poll loop never calls `tick_zombie_check`, unlike `run()` | 2026-07-15 | 2026-07-16 | src/bin/bambino-cli/monitor/mod.rs:54-59 — ping-tick branch now calls `printer.mqtt().await?.tick_zombie_check(PING_TICK_SECS as u32)?` after `send_ping()`, mirroring `run()`'s dashboard loop |
| BUG-130 | Sev3 | bin/bambino-cli/connection.rs, camera/binary.rs | `validate_params`'s access-code check disagreed with the camera protocol's own constraint | 2026-07-15 | 2026-07-16 | src/bin/bambino-cli/connection.rs:76-89 — ceiling raised to `CAMERA_PASSWORD_MAX_LEN` (32, promoted to `pub` in camera/binary.rs) plus an alphanumeric check, aligning the CLI's check with camera/rtsps.rs's downstream requirement |
| BUG-131 | Sev3 | bin/bambino-cli/main.rs | `Control`'s `override_usage` mislabeled the AMS dry-cycle duration arg | 2026-07-15 | 2026-07-16 | src/bin/bambino-cli/main.rs:121 — `<TIME>` relabeled `<HOURS>`, matching BUG-118's `duration_hours` field rename |
| BUG-132 | Sev3 | discovery/parser.rs | `SsdpDevice::raw_model_str`'s doc comment overstated its provenance | 2026-07-15 | 2026-07-16 | src/discovery/parser.rs:35 — reworded to note the NT/ST URN-derived fallback case, not just the unmodified `DevModel` header |
| BUG-133 | Sev3 | ftps/parser.rs | `parse_unix_listing`'s `day` bound wasn't month-aware, sibling gap to BUG-061 | 2026-07-15 | 2026-07-16 | src/ftps/parser.rs — added `days_in_month()` (leap-year-aware) and a post-rollover `day > days_in_month(month, year)` check using the finalized `year` |
| BUG-134 | Sev3 | mqtt/client/codec.rs | Wire-encoding length prefixes silently truncated instead of erroring past `u16::MAX` | 2026-07-15 | 2026-07-16 | src/mqtt/client/codec.rs — added `debug_assert!`s on `client_id`/`username`/`password`/`topic` lengths before each `as u16` cast, as insurance (not reachable today) |
| BUG-135 | Sev3 | client/telemetry.rs | The BUG-110-corrected `is_ethernet_active()` was unreachable through `PrinterClient` | 2026-07-15 | 2026-07-16 | src/client/telemetry.rs — added `TelemetryCache::last_net_conf`, populated from `print.net.conf`, plus `PrinterClient::is_ethernet_active()` reading bit 0 |
| BUG-136 | Sev3 | src/ftps/CLAUDE.md | Nested CLAUDE.md invariant text was stale re: `download_file`'s accepted reply codes | 2026-07-15 | 2026-07-16 | src/ftps/CLAUDE.md — reworded "exact-`226` requirement" to "acceptance of both `226` and the documented `426` close-race reply", matching BUG-030 |
| BUG-137 | Sev3 | src/types/telemetry/CLAUDE.md | Nested CLAUDE.md invariant text incompletely listed `unpack_temperature()`'s use sites | 2026-07-15 | 2026-07-16 | src/types/telemetry/CLAUDE.md — added `unpack_bed_telemetry()`'s use for `device.bed.info.temp` as the third use site |
| BUG-138 | Sev3 | discovery/mod.rs | `MULTICAST_IP`/`MULTICAST_ADDR` were two independently-maintained literals for the same address | 2026-07-15 | 2026-07-16 | src/discovery/mod.rs:26-27 — deleted `MULTICAST_IP`, made `MULTICAST_ADDR` the sole `pub` constant |
| BUG-139 | Sev3 | Cargo.toml | `no_std` Cargo feature was vestigial — never read anywhere | 2026-07-15 | 2026-07-16 | Cargo.toml — removed the dead `no_std = []` feature and its reference from `embassy`'s feature list; `#![no_std]` gating in lib.rs already keys off `not(feature = "std")` |
| BUG-140 | Sev3 | mqtt/client/mod.rs | Stalled-connection/partial-frame resumability invariant was untested through the persistent `BambuMqttClient` | 2026-07-15 | 2026-07-16 | src/mqtt/client/mod.rs — added `test_poll_telemetry_with_timer_resumes_split_frame_through_persistent_client` (`#[cfg(feature = "tokio")]`), splitting a real PUBLISH frame across two writes with a real sleep between them through `poll_telemetry_with_timer(&TokioTimer::new())` |
| BUG-126 | Sev3 | types/telemetry/ams.rs | `AmsTray` missing `remain_g`/`setting_id` accessors | 2026-07-13 | 2026-07-13 | Confirmed via BambuStudio commit `31637e013` (2026-06-09) and `DevFilaSystem.cpp:800-801`. Added `AmsTray::remain_g`/`filament_setting_id` plus `remaining_weight_grams()` (sentinel-translates `remain_g <= 0` to `None`). No capture confirms firmware sends it yet — both fields `Option`, safe regardless. |
| BUG-122 | Sev3 | ams/mapping.rs, quirks/mod.rs, quirks/models/*.rs | Outbound AMS construction didn't validate a config against the model's actual pool structure | 2026-07-12 | 2026-07-12 | Confirmed against `MODEL_MATRIX.csv`'s "AMS Unit Limits" row. Added `AmsPoolComposition` enum, `ModelQuirks::ams_pool_composition()` (implemented per model), and `ams::validate_ams_pool_composition()`. AMS Lite isn't independently addressable by `ams_id`, so A1's exclusive-OR and A2L's additive lite slot are conservatively folded into the plain 4-unit shared pool (documented limitation, never accepts an actually-invalid config) |
| BUG-124 | Sev3 | ams/parser.rs, types/telemetry/device.rs, client/telemetry.rs | `resolve_printing_global_id` had zero callers; preferred `snow`/`spre`/`star` decode didn't exist | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevExterSystem.cpp:318-386`, 8/8 split confirmed by BUG-112). Added `ExtruderInfo::current/previous/target_ams_slot()` and a real caller, `PrinterClient::printing_tray_global_id()`, using the preferred `snow`-direct method (no `ams_extruder_map` needed). Map-based `resolve_printing_global_id` documented as fallback-only; `ams_extruder_map`'s own wire construction remains unconfirmed, out of scope here |
| BUG-123 | Sev3 | types/telemetry/report.rs | No accessor decoded `home_flag` bits 8-9 (SD-card state) | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DeviceManager.cpp:1092`) and pybambu. Added `SdcardState` enum + `PrinterTelemetry::sdcard_state()`; `sdcard` bool alone can never report a degraded card |
| BUG-120 | Sev3 | types/telemetry/ams.rs | `AmsUnit` missing `dry_fan1_status`/`dry_fan2_status` accessors | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevFilaSystem.cpp:696-697`) and `bambu-printer-manager`, bits 18-19/20-21, mask `0x3` each; added both accessors |
| BUG-121 | Sev3 | types/telemetry/ams.rs | `AmsStatusReport` missing `calibrate_remain_flag`/`.cfs` | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevFilaSystem.cpp:507-508,540-541`). Added `calibrate_remain_flag: Option<bool>` and `cfs: Option<Vec<AmsFilamentStep>>` (new typed enum mirroring `DevFilamentStep`, `DevDefs.h:64`) |
| BUG-110 | Sev3 | types/telemetry/report.rs | `is_ethernet_active()`'s bit-18 `home_flag` heuristic confirmed wrong (real bit is prompt-sound-detection support) | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevPrintOptions.cpp:26`, `DeviceManager.cpp:3053`). Added `PrinterTelemetry.net: Option<NetInfo>` + rewrote `is_ethernet_active()` to read `print.net.conf` bit 0, the confirmed-authoritative source; `reference/03_mqtt_telemetry.md` and this dir's `CLAUDE.md` corrected |
| BUG-112 | Sev3 | types/telemetry/device.rs | `ExtruderInfo.snow` doc comment claimed a 4-bit split; real wire layout is 8/8 (slot_id/ams_id) | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevExtruderSystem.cpp:369-372`); doc comments corrected for `snow`/`spre`/`star` (doc-only, no decode helper exists yet — see BUG-124) |
| BUG-119 | Sev2 | mqtt/commands/print_job.rs | `ProjectFilePayload` was missing `flow_cali`/`profile_id`/`project_id`/`task_id` | 2026-07-12 | 2026-07-12 | Confirmed against bambuddy source directly (`bambu_mqtt.py:3721-3781`): mints one fresh ID per submission, reused for `subtask_id`/`project_id`/`task_id` alike. Added all 4 fields; `project_id`/`task_id` set equal to `subtask_id` (already contractually fresh-per-submission in bambino) rather than a second ID-minting mechanism |
| BUG-118 | Sev2 | mqtt/commands/ams.rs, client/ams.rs | `AmsFilamentDryingPayload` sent the wrong wire field names entirely | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevFilaSystemCtrl.cpp:18-53`) and bambuddy. Rewrote to `mode`/`filament`/`temp`/`duration`(hours)/`humidity`/`rotate_tray`/`cooling_temp`/`close_power_conflict`; `start_drying()`/CLI `ams dry` signatures changed accordingly (pre-1.0 breaking change) |
| BUG-116 | Sev2 | mqtt/commands/ams.rs, client/ams.rs | `AmsChangeFilamentPayload::target` doc claimed it mirrors `slot_id`; real formula depends on unit type | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source directly (`DeviceManager.cpp:1602-1638`). `PrinterClient::change_filament()` now derives `target` internally (dropped the caller-supplied `target` param — pre-1.0 breaking change) instead of trusting an easily-wrong value; fixed 2 wrong worked examples in `reference/05_materials_ams.md` |
| BUG-117 | Sev2 | mqtt/commands/ams.rs, client/ams.rs | `AmsFilamentSettingRequest::new` doc claimed IDEX external-spool `tray_id` is `0`; BambuStudio always sends `254` | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DeviceManager.cpp:1667-1693`); doc comments corrected in both files plus `reference/05_materials_ams.md`/`reference/03_mqtt_telemetry.md` |
| BUG-111 | Sev2 | types/telemetry/mod.rs, types/telemetry/device.rs | IDEX-detection heuristic (`nozzle.info.len() >= 2`) misclassifies an H2C rack-spare nozzle as IDEX | 2026-07-12 | 2026-07-12 | Verified against BambuStudio source (`DevNozzleSystem.cpp:769`): `get_hex_bits(id,1)==1` is a 4-bit nibble check, not bit 1. Added `NozzleInfo::is_rack_stored()`, excluded from the IDEX count; commit `a083942` |
| BUG-114 | Sev2 | ams/parser.rs | `evaluate_spool_presence` returns `None` for the entire AMS-HT range; reopens BUG-015 | 2026-07-12 | 2026-07-12 | AMS-HT now computes `16 + (ams_id-128) + slot_id` per BambuStudio's `DevAms::GetTrayId`; `reference/05_materials_ams.md` corrected; commit `b8b9c38` |
| BUG-125 | Sev2 | ams/parser.rs | `AMS_MAX_STANDARD_ID = 7` wrong; reverts to `3`, reopens/reverses BUG-068 | 2026-07-12 | 2026-07-12 | `AMS_MAX_STANDARD_ID` reverted `7`→`3`; three independent sources agree standard units cap at 4 (see BUG-068 row); commit `b8b9c38` |
| BUG-108 | Sev2 | diagnostics/hms.rs | `HmsSeverity::from_attr` derives severity from `attr`, but the wire's actual severity lives in `code >> 16` | 2026-07-12 | 2026-07-12 | Renamed to `HmsSeverity::from_code(code)`, `(code >> 16) & 0xFFFF`; `reference/07_diagnostics_hms.md` corrected; commit `9e5ece3` |
| BUG-109 | Sev2 | diagnostics/hms.rs | `is_status_step` in `decode_hms_alert` compares `code_low` (low 16 bits); should compare the full 32-bit `code`, per BambuStudio's own bundled fault catalog | 2026-07-12 | 2026-07-12 | `decode_hms_alert`'s `is_status_step` now compares full `code`; `decode_print_error`'s single-register check left as-is; commit `9e5ece3` |
| BUG-105 | Sev3 | types/telemetry/diagnostics.rs, client/telemetry.rs | `IpcamTelemetry` has no `merge_from` and no cache field — camera/recording telemetry is never persisted across pushes | 2026-07-12 | 2026-07-12 | Added `IpcamTelemetry::merge_from` (preserve-on-absence, matches BUG-096/095/097) and `TelemetryCache::last_ipcam` + `PrinterClient::ipcam()`; commit `5480eeb` |
| BUG-106 | Sev2 | types/telemetry/diagnostics.rs | `HmsEntry.attr`/`.code` are required `u32`, so one malformed/hex-string entry fails the entire telemetry message's deserialize | 2026-07-12 | 2026-07-12 | Added `deserialize_permissive_hms_u32` (accepts int or hex string, defaults `0` instead of failing the whole message), matching BambuStudio's default-zeroed-item behavior; commit `5480eeb` |
| BUG-107 | Sev3 | types/telemetry/diagnostics.rs | `HmsEntry.ts_boot`/`.ts_unix` confirmed real (X2 only); doc's "present on X2/H2/P2" overclaims H2/P2 | 2026-07-12 | 2026-07-12 | Doc comment narrowed to "confirmed on X2, unverified on H2/P2"; commit `5480eeb` |
| BUG-104 | Sev2 | types/telemetry/ams.rs | `AmsUnit::dry_sub_status()` reads a 4-bit mask instead of the real 2-bit wire field | 2026-07-12 | 2026-07-12 | `AMS_UNIT_INFO_DRY_SUB_STATUS_MASK` narrowed `0xF`→`0x3` (bits 22-23 only; 24-25 belong to unrelated `bind_switch_in`), doc comments and `reference/05_materials_ams.md` corrected; commit `64bf3e7` |
| BUG-001 | Sev2 | mqtt/commands, diagnostics/kprofile | `sequence_id` unclamped in 6 public command constructors | 2026-07-09 | 2026-07-09 | 6 constructors serialized `sequence_id`/`subtask_id` without `clamp_task_id()`, risking the documented 32-bit firmware lockup — fixed in `0ae1e51` |
| BUG-002 | Sev3 | mqtt/client/mod.rs | `get_in_flight_count` doc comment says slice, returns `usize` | 2026-07-09 | 2026-07-09 | mqtt/client/mod.rs:501 — doc comment said "returns a slice," function returns `usize` — corrected in `93e6ca1` |
| BUG-003 | Sev2 | ftps/client.rs | `download_file` has no integrity recheck (unlike `upload_file`'s SIZE recheck) | 2026-07-09 | 2026-07-09 | ftps/client.rs:600-669 `download_file` had no SIZE-recheck symmetric to `upload_file`'s, so a truncated transfer could report success — fixed in `a46281f` |
| BUG-004 | Sev3 | ftps/client.rs | 6 single-reply commands don't poison on `read_response`/`write_command` failure | 2026-07-09 | 2026-07-09 | 6 single-reply methods didn't poison the client on failure — poisoned in `403655f`, matching the transfer-method precedent; user chose poisoning (deterministic) over the cheaper drain-and-continue alternative |
| BUG-005 | Sev3 | camera/rtsps.rs | IPv6 addresses not bracketed in RTSPS URLs | 2026-07-09 | 2026-07-09 | camera/rtsps.rs `build_rtsps_url`/`rewrite_rtsp_request_uri` didn't bracket an IPv6 host — fixed in `db6cb10` |
| BUG-006 | Sev3 | camera/binary.rs | `build_handshake_packet` accepts empty access code | 2026-07-09 | 2026-07-09 | camera/binary.rs `build_handshake_packet`'s alphanumeric check passed vacuously on an empty string — fixed in `5bdc364` |
| BUG-007 | Sev3 | client/connect.rs | `with_connect_timeout(0)` fails instantly instead of disabling the timeout | 2026-07-09 | 2026-07-09 | client/connect.rs `race_against_connect_timeout` treated `connect_timeout_secs == 0` as an instant deadline — fixed in `957d747` |
| BUG-008 | Sev3 | io/tokio.rs | `CnFallbackServerVerifier` ignores intermediate certs (single-hop chain only) | 2026-07-09 | 2026-07-09 | io/tokio.rs:257 `verify_server_cert` ignored `_intermediates`, single-hop chains only — now walks the full chain, fixed in `6316561` |
| BUG-009 | Sev2 | discovery/mod.rs | Degraded-mode SSDP bind is order-dependent, not "try all ports" | 2026-07-09 | 2026-07-09 | discovery/mod.rs:176-190 returned `Err` on the first port's bind failure instead of trying the rest — fixed in `91e25f3` |
| BUG-010 | Sev2 | discovery/mod.rs | Initial broadcast loop aborts whole sweep on one engine's send failure | 2026-07-09 | 2026-07-09 | discovery/mod.rs:203-207 — one engine's send failure aborted the whole sweep — fixed in `478df11` |
| BUG-011 | Sev2 | discovery/parser.rs | SSDP-discovered serial not uppercased despite doc promise | 2026-07-09 | 2026-07-09 | discovery/parser.rs `parse_ssdp_payload` didn't uppercase the discovered serial — fixed in `ab7f8c2` |
| BUG-012 | Sev2 | ams/parser.rs | State-10 tray-clearing: doc said clear, code didn't | 2026-07-09 | 2026-07-09 | ams/parser.rs:112-116 `clean_stale_tray_data` — state 10 alone didn't clear stale tray data — fixed in `66f3f6c`; resolved via pybambu/Bambuddy cross-check (no H2D hardware available), see `reference/05_materials_ams.md`'s verification-source note |
| BUG-013 | Sev3 | error.rs | `no_std` `Display` impl sync unverified by any test that actually runs | 2026-07-09 | 2026-07-09 | error.rs — `test_display_consistency` only runs under `std`, so the `no_std` impl's sync was never test-verified, just manually inspected — doc comment corrected in `f288cd8` (process gap, no `no_std` test harness exists to fix it properly) |
| BUG-014 | Sev2 | ams/parser.rs | `evaluate_spool_presence` doesn't bounds-check `tray_id` before a bit-shift | 2026-07-10 | 2026-07-10 | src/ams/parser.rs:60-61 — added `tray_id >= AMS_SLOTS_PER_UNIT` guard alongside the existing `ams_id` check |
| BUG-015 | Sev2 | ams/parser.rs | AMS-HT slots always report `Some(true)` presence regardless of actual state — since superseded, see BUG-114 | 2026-07-10 | 2026-07-10 | Changed to return `None` (unknown) for AMS-HT, but that premise was itself wrong — reopened and replaced with a real bit-offset formula by BUG-114 (Fixed), which supersedes this row |
| BUG-018 | Sev2 | client/connect.rs | No `disconnect_mqtt()`/`attach_mqtt()` — dead MQTT session has no recovery path | 2026-07-10 | 2026-07-10 | src/client/connect.rs — added `attach_mqtt()`/`disconnect_mqtt()` mirroring the camera/storage pair |
| BUG-019 | Sev2 | client/connect.rs | Sequence-counter reseed is a no-op under the default `DummyTimer` | 2026-07-10 | 2026-07-10 | src/client/connect.rs:110-113 — reseed now gated on `timer.has_real_clock()`, matching `race_against_connect_timeout`'s existing guard |
| BUG-020 | Sev2 | client/connect.rs | `ensure_ftps`/`ensure_camera` consume config even on a failed connect attempt | 2026-07-10 | 2026-07-10 | src/client/connect.rs, src/ftps/client.rs — camera borrows tls/factory instead of owning them until success; FTPS's `connect()` split into borrow-based `connect_control_stream()` + `from_control_stream()` so `ftps_config` isn't `.take()`n until the handshake actually succeeds |
| BUG-021 | Sev2 | client/telemetry.rs | `last_door_open` cache overwritten unconditionally, ignoring absent-field staleness contract | 2026-07-10 | 2026-07-10 | src/client/telemetry.rs:156, src/quirks/{mod.rs,models/{x1,h2,p2,x2}.rs} — added `ModelQuirks::door_sensor_field_present()` (home_flag for X1, stat for H2/P2/X2) to gate the cache update precisely, instead of an imprecise "either field present" shortcut |
| BUG-024 | Sev2 | discovery/mod.rs | `poll_next_device` never stamps `discovery_port` | 2026-07-10 | 2026-07-10 | src/discovery/mod.rs — `poll_next_device` now stamps `device.discovery_port = self.port` itself before returning, instead of relying on the `discover_devices()` wrapper; required for Embassy callers, which drive `DiscoveryEngine` directly |
| BUG-031 | Sev2 | io/esp_idf.rs | `EspIdfTcpStream` Read/Write block with no preempt point | 2026-07-10 | 2026-07-10 | src/io/esp_idf.rs — socket now stays non-blocking past `connect()`; `read()`/`write()` retry on `WouldBlock` via a new `retry_on_would_block_io()` helper pacing on `EspIdfTimer::sleep(TLS_POLL_INTERVAL)`, mirroring `EspTlsStream`'s existing pattern; verified via `scripts/check-esp-idf.sh esp32c6` |
| BUG-033 | Sev2 | mqtt/commands/print_job.rs | `with_ams_mapping2()` doesn't sync `ams_mapping`, breaking documented 1:1 pairing | 2026-07-10 | 2026-07-10 | src/ams/mapping.rs, src/mqtt/commands/print_job.rs — added `flat_channel_id_for_entry()`; `from_config` now derives the flat `ams_mapping` array from `ams_mapping2` whenever the latter is the active source, instead of trusting the separately-set `config.ams_mapping` |
| BUG-016 | Sev3 | bin/bambino-cli/main.rs | `--help` missing `gcode-raw --unsafe` documentation | 2026-07-10 | 2026-07-10 | src/bin/bambino-cli/main.rs:49 — added `--unsafe`/confirmation-prompt lines to `after_help` |
| BUG-025 | Sev3 | discovery/mod.rs | Module doc wrongly claims `discover_devices()` works on Embassy | 2026-07-10 | 2026-07-10 | src/discovery/mod.rs:1-12 — reworded to attribute cross-platform support to `DiscoveryEngine`, note `discover_devices()` is std-only |
| BUG-026 | Sev3 | lib.rs | Docs claim embassy TLS backend is `embedded-tls`, actually `mbedtls-rs` | 2026-07-10 | 2026-07-10 | src/lib.rs:16,65 — both occurrences corrected to `mbedtls-rs` |
| BUG-027 | Sev3 | lib.rs | Feature Flags table wrongly claims `tokio` enables the CLI binary | 2026-07-10 | 2026-07-10 | src/lib.rs:60-67 — removed "CLI binary" from the `tokio` row, added a `cli` row |
| BUG-017 | Sev3 | bin/bambino-cli/probe.rs | Capture-window error discards all previously-captured probe results | 2026-07-10 | 2026-07-10 | src/bin/bambino-cli/probe.rs — `capture_responses()` failures now recorded as a per-entry `capture_error` field instead of propagating via `?`, mirroring `publish_error`'s existing pattern; the run continues and still writes the report |
| BUG-022 | Sev3 | tests/client_test.rs | `test_sequence_id_wrapping` never exercises wraparound | 2026-07-10 | 2026-07-10 | tests/client_test.rs (renamed `test_sequence_id_fits_in_i32`), src/mqtt/commands/mod.rs — added `test_clamp_task_id_wraps_near_max` colocated with `clamp_task_id()`, since `sequence_counter` is `pub(crate)` and unreachable from the external integration test |
| BUG-023 | Sev3 | tests/client_test.rs | No test coverage for X1C's voltage-dependent bed-temp ceiling | 2026-07-10 | 2026-07-10 | tests/client_test.rs — added `test_x1c_bed_temp_ceiling_voltage_dependent`, feeding `home_flag` telemetry via `poll_telemetry()` and asserting all three branches (pre-telemetry conservative default, confirmed 220V, confirmed 110V) |
| BUG-028 | Sev3 | ftps/protocol.rs | `read_response` doesn't follow RFC 959 multi-line reply parsing | 2026-07-10 | 2026-07-10 | src/ftps/protocol.rs — now tracks the reply's opening code; only a line with the same code + space terminates, same code + hyphen continues in this parser's padded format, anything else is preserved verbatim as free-text body |
| BUG-029 | Sev3 | ftps/client.rs | LIST/STOR/RETR initial write/read not poisoned on failure | 2026-07-10 | 2026-07-10 | src/ftps/client.rs — LIST/STOR/RETR's initial `write_command`/`read_response` now poison on `Err`, matching every other control-channel operation per `.claude/rules/ftps-poisoning.md` |
| BUG-030 | Sev3 | ftps/client.rs | `download_file`'s 426→SIZE recheck isn't symmetric with `upload_file`'s | 2026-07-10 | 2026-07-10 | src/ftps/client.rs — `download_file`'s confirmation check now accepts `FTP_TRANSFER_ABORTED` (426) alongside `FTP_TRANSFER_COMPLETE` before the SIZE recheck, matching `upload_file`'s existing handling |
| BUG-032 | Sev3 | mqtt/client/mod.rs | CONNACK codes 1-3 collapsed into `AccessDenied` along with 4-5 | 2026-07-10 | 2026-07-10 | src/mqtt/client/mod.rs — only codes 4/5 (bad credentials/not authorized) now map to `AccessDenied`; codes 1-3 map to `ProtocolViolation` with the raw code preserved |
| BUG-034 | Sev3 | types/telemetry/mod.rs | No `fun()` merging accessor despite documented dual-location drift | 2026-07-10 | 2026-07-10 | src/types/telemetry/mod.rs — added `TelemetryReport::fun()` mirroring `device()`'s fallback order (top-level first, then `print.fun`) |
| BUG-035 | Sev3 | types/telemetry/tests.rs | `test_ams_unit_info_bitmask` doesn't call the accessors it claims to verify | 2026-07-10 | 2026-07-10 | src/types/telemetry/tests.rs — replaced the hand-rolled bit math with real `unit.ams_type()`/`unit.extruder_assignment()` calls |
| BUG-036 | Sev3 | types/telemetry/mod.rs | `decode_nozzle_temperatures()` has zero test coverage | 2026-07-10 | 2026-07-10 | src/types/telemetry/tests.rs — added tests for all three branches (composite `device.extruder.info`, single-nozzle flat fallback, IDEX swapped-fallback quirk) |
| BUG-037 | Sev3 | types/telemetry/report.rs | `is_220v_power()` has zero test coverage despite gating a safety ceiling | 2026-07-10 | 2026-07-10 | src/types/telemetry/tests.rs — added `test_is_220v_power_from_home_flag` (set/clear/missing), mirroring `test_door_open_from_home_flag` |
| BUG-038 | Sev3 | tests/common/mock_ftps.rs | `read_cmd`'s single read can't detect a `write_command` framing regression | 2026-07-10 | 2026-07-10 | tests/common/mock_ftps.rs — documented that `read_cmd` isn't a safety net for this invariant (tokio duplex coalesces split writes before it's ever polled); the real guard is `protocol.rs`'s `WriteRecorder`-based unit test |
| BUG-039 | Sev2 | ams/mapping.rs | `validate_external_spool_safety`'s external-spool check misses `ams_id` 254 | 2026-07-10 | 2026-07-10 | src/ams/mapping.rs:184-196 — `is_external` now checks `ams_id` 254 or 255 (was 255 only); `AmsMapping2Entry`'s fields are public so a caller can hand-build a 254 entry on single-nozzle, previously leaving `use_ams:true` on a non-physical channel — reproduces the `07FF_8012` lockup class; commit `746f018` |
| BUG-040 | Sev2 | camera/binary.rs | Oversized-frame rejection doesn't drain the declared payload, desyncing the stream | 2026-07-10 | 2026-07-10 | src/camera/binary.rs — added `CameraFrameReadState::DiscardingOversizedPayload`/`drain_oversized_payload()`; drains the declared-but-rejected payload in bounded chunks (never allocating it) instead of resetting straight to `Idle`, so a caller retrying on the same instance reads the next real frame instead of misreading stale bytes; commit `9b7a2af` |
| BUG-041 | Sev3 | camera/rtsps.rs | `build_rtsps_url`/`rewrite_rtsp_request_uri` hardcode literal `322` instead of `CAMERA_PORT_RTSPS` | 2026-07-10 | 2026-07-10 | src/camera/rtsps.rs — both call sites now use `crate::camera::CAMERA_PORT_RTSPS`; commit `9b7a2af` |
| BUG-043 | Sev3 | bin/bambino-cli/monitor/mod.rs | `TerminalGuard::enter()` leaves raw mode enabled if the alt-screen write fails | 2026-07-10 | 2026-07-10 | src/bin/bambino-cli/monitor/mod.rs:52-64 — `Self` is now constructed immediately after `enable_raw_mode()` succeeds, before the fallible write/flush, so `Drop` still restores the terminal on that path; commit `ee875d8` |
| BUG-044 | Sev3 | bin/bambino-cli/control.rs | `ControlAction::Move`'s `axis` was a free-form `String`, unlike every other CLI arg using `ValueEnum` | 2026-07-10 | 2026-07-10 | src/bin/bambino-cli/control.rs — added `AxisArg` (`ValueEnum`, X/Y/Z), replacing the runtime `len() != 1` check with clap-level validation; commit `ee875d8` |
| BUG-045 | Sev3 | tests/client_test.rs | `test_in_flight_saturation` only asserts `is_err()`, not the specific variant | 2026-07-10 | 2026-07-10 | tests/client_test.rs:736-745 — now asserts `NetworkError(SocketError::TimedOut)`, matching `publish_command`'s documented in-flight-saturation response and the sibling `test_connection_drop_during_operation`'s stricter pattern; commit `ddb9dca` |
| BUG-046 | Sev3 | discovery/mod.rs | `discover_devices()` poll loop silently discards `poll_next_device` errors, no backoff | 2026-07-10 | 2026-07-10 | src/discovery/mod.rs — added logging + a `SSDP_POLL_ERROR_BACKOFF_MS` (50ms) pacing sleep on the `Err` branch, preventing a persistently-erroring socket from busy-spinning the rest of the discovery window with zero operator-visible signal; commit `2da9127` |
| BUG-047 | Sev3 | discovery/parser.rs | Present-but-empty `DevModel.bambu.com` header blocks the NT/ST model fallback | 2026-07-10 | 2026-07-10 | src/discovery/parser.rs:202-208 — `effective_dev_model` now `.filter(|s| !s.is_empty())`s the header before `.or_else()`, so `Some("")` no longer short-circuits the fallback; commit `2da9127` |
| BUG-048 | Sev3 | io/tokio.rs | `CnFallbackServerVerifier` chain-walk gives up on the first same-subject intermediate instead of trying others | 2026-07-11 | 2026-07-11 | src/io/tokio.rs:310-320 — narrower BUG-008 shape (duplicate-subject-name intermediates), fail-closed only (spurious rejection, never a bad-cert accept); now tries every unused same-subject candidate's signature instead of just the first by position; commit `c0e8022` |
| BUG-049 | Sev3 | io/embassy.rs | `EmbassyUdpSocket::send_to`/`recv_from` collapsed every `SendError`/`RecvError` variant to `ConnectionReset` | 2026-07-11 | 2026-07-11 | src/io/embassy.rs:53-100 — now maps `SendError::SocketNotBound`→`NotConnected`, `PacketTooLarge`→`InvalidInput`, and `SendError::NoRoute`/`RecvError::Truncated` (no matching `SocketError` variant) → `Other` with a descriptive message, instead of a uniform misleading `ConnectionReset`; commit `104e22b` |
| BUG-050 | Sev3 | io/embassy.rs | `EmbassyRawStreamFactory::dial` mapped its single-variant `tcp::Error` (`ConnectionReset`, covers both a real RST and pool exhaustion) to a fabricated `ConnectionRefused` | 2026-07-11 | 2026-07-11 | src/io/embassy.rs:274-311 — now maps to `SocketError::ConnectionReset`, matching the actual source variant instead of inventing a distinction `embassy-net` 0.9.1's `tcp::client::Error` doesn't make; could have misrouted `SocketError`-keyed retry/backoff decisions; commit `104e22b` |
| BUG-052 | Sev3 | mqtt/client/ | `push_pending`'s cap could be silently exceeded by one oversized message; QoS 2 PUBLISH never PUBREC'd | 2026-07-11 | 2026-07-11 | src/mqtt/client/pending.rs — added a `const _` assert linking `MQTT_MAX_PAYLOAD_BYTES` to `MQTT_PENDING_BUFFER_MAX_BYTES` so they can't silently drift apart. QoS 2 PUBLISH now `log::warn!`s instead of silently no-op'ing (full PUBREC/PUBREL/PUBCOMP handshake intentionally not implemented, user declined); commit `5e550d1`. |
| BUG-053 | Sev3 | mqtt/commands/ | Task-ID clamping enforced only by convention across 24 constructors in 7 files, same shape as BUG-001 | 2026-07-11 | 2026-07-11 | src/mqtt/commands/mod.rs — added `ClampedTaskId`, obtainable only via its clamping `From<u64>` impl; every constructor's `sequence_id: u64` param is now `impl Into<ClampedTaskId>`, so skipping the clamp is no longer expressible rather than merely test-covered (2 of 24 previously); user chose the type-safety fix over extending the regression test; commit `03a7dc3` |
| BUG-054 | Sev3 | types/telemetry/device.rs | `DeviceTelemetry.bed_temp` parsed off the wire but never consulted; now confirmed redundant, documented | 2026-07-11 | 2026-07-11 | Cross-referenced against pybambu (`models.py`) and Bambuddy, neither consults `device.bed_temp`; documented as confirmed-redundant in doc comment + `reference/04_toolhead_thermal_motion.md`, commit `95774a8`. Reopened then re-closed with stronger evidence — see BUG-113 (Wontfix). |
| BUG-055 | Sev3 | tests/common/ | Mock camera/FTPS harnesses had no coverage for multi-chunk upload reassembly, rejected handshake, or mid-frame disconnect | 2026-07-11 | 2026-07-11 | Added `test_ftps_upload_multi_chunk_reassembles_correctly` (byte-for-byte reassembly past one write-loop iteration) and two camera tests (rejected access code, mid-payload disconnect); commit `cfb6624`. |
| BUG-042 | Sev3 | ftps/parser.rs, bin/bambino-cli/storage.rs | `list_directory`'s year-rollover math anchors to host UTC; printer's own clock (source of raw `LIST` HH:MM timestamps) is often unsynced | 2026-07-10 | 2026-07-11 | Root cause confirmed unfixable (no RTC, unreliable LAN-mode NTP). Added `FtpFile::year_is_inferred`; `files list` marks inferred rows and points at `files clock-check`. Verified against real P1S; commit `18446b3`. |
| BUG-056 | Sev2 | client/connect.rs, diagnostics/kprofile.rs | `k_profile_primed` not reset on `disconnect_mqtt()` | 2026-07-11 | 2026-07-11 | src/client/connect.rs:151 — `disconnect_mqtt()` now resets `k_profile_primed = false` alongside clearing `self.mqtt`, so a reconnect re-primes `get_k_profiles()` instead of skipping the firmware priming quirk and hanging until `command_timeout_secs` |
| BUG-065 | Sev1 | quirks/mod.rs | `format_z_move_gcode` doesn't reject `NaN` distance | 2026-07-11 | 2026-07-11 | src/quirks/mod.rs:261 — `NaN` failed both the `== 0.0` and `.abs() > z_max` guards (both false for `NaN`), letting a malformed `G0 ZNaN` reach the printer; now gated on `!distance.is_finite()` first, also rejecting `±INFINITY` |
| BUG-060 | Sev2 | discovery/parser.rs | `parse_ssdp_payload` accepted non-Bambu SSDP devices | 2026-07-11 | 2026-07-11 | src/discovery/parser.rs:213-224 — now rejects packets with neither a recognized `model` (serial prefix or `DevModel`) nor an NT/ST urn containing `bambulab-com`; previously any USN+LOCATION SSDP packet (routers, TVs, other vendors' printers) parsed successfully and could appear in `discover_devices()` results |
| BUG-068 | Sev2 | ams/parser.rs, ams/mapping.rs | `AMS_MAX_STANDARD_ID = 3` (4 units) too low — since superseded, see BUG-125 | 2026-07-11 | 2026-07-11 | Raised to `7`, but that widening was itself wrong — reopened and reverted back to `3` by BUG-125 (Fixed), which supersedes this row's justification |
| BUG-057 | Sev3 | bin/bambino-cli/storage.rs | `run_clock_check` silently drops the cleanup-delete's error when listing also fails | 2026-07-11 | 2026-07-11 | src/bin/bambino-cli/storage.rs:206-219 — when `listing` fails, `delete_result`'s error (if any) is now printed via `eprintln!` before propagating the listing error, instead of being silently discarded |
| BUG-058 | Sev3 | bin/bambino-cli/main.rs | `control ams` usage lines omitted `<IP> <SERIAL> [ACCESS_CODE]` | 2026-07-11 | 2026-07-11 | src/bin/bambino-cli/main.rs:116 — the 3 `control ams ...` `override_usage` lines now include `<IP> <SERIAL> [ACCESS_CODE]`, matching every other subcommand line, same class as BUG-016 |
| BUG-059 | Sev3 | tests/client_test.rs | No test asserted the `extrude_cali_flag` wire field | 2026-07-11 | 2026-07-11 | tests/client_test.rs:806 — `test_start_print_wire_payload` now asserts `extrude_cali_flag: 1`, matching `PrintJobConfig::new()`'s README-documented `run_flow_calibration: true` default |
| BUG-061 | Sev3 | ftps/parser.rs | `parse_unix_listing` didn't range-validate `day`/`hour`/`minute` | 2026-07-11 | 2026-07-11 | src/ftps/parser.rs:144-171 — `day` now rejected outside `1..=31`, `hour`/`minute` outside `0..=23`/`0..=59` (skipping the entry, matching the file's existing silent-skip convention), unlike `month`'s already-validated lookup table |
| BUG-062 | Sev3 | ftps/protocol.rs | `read_response`'s header-establishing branch silently dropped a non-conformant reply line | 2026-07-11 | 2026-07-11 | src/ftps/protocol.rs:254-268 — a header line whose 4th byte is neither `' '` nor `'-'` (e.g. `"200\r\n"`, code immediately followed by CRLF) now returns a terminal reply with empty text instead of falling through to `continue` and being silently discarded |
| BUG-075 | Sev3 | ftps/protocol.rs | `validate_ftp_path`'s leading-dash check missed a trailing-slash path | 2026-07-11 | 2026-07-11 | src/ftps/protocol.rs:381-386 — `.next_back()` alone returned `""` for a trailing-slash path (e.g. `/cache/-dir/`), silently skipping the check; now `.rev().find(|s| !s.is_empty())` finds the real final segment first |
| BUG-063 | Sev3 | io/embassy.rs | `EmbassyTlsConnector::connect` collapsed every mbedtls-rs failure with no logging | 2026-07-11 | 2026-07-11 | src/io/embassy.rs:196-224 — all 3 sites (`Session::new`, `set_server_name`, `Session::connect`) now `log::debug!` the real mbedtls-rs error before mapping to `SocketError::ConnectionAborted` |
| BUG-064 | Sev3 | io/esp_idf.rs | `EspTls::adopt()` failure discarded the real `EspError`, no logging | 2026-07-11 | 2026-07-11 | src/io/esp_idf.rs:701-708 — now `log::debug!`s the real `EspError` before mapping to `SocketError::Other`, consistent with `map_esp_tls_connect_error` and every other FFI error site in the file |
| BUG-076 | Sev3 | io/esp_idf.rs | 3 more `EspIdfTimer::new()` sites discarded the real `EspError`, no logging | 2026-07-11 | 2026-07-11 | src/io/esp_idf.rs:83-85,498-500,699-701 (UDP pacing, TCP connect, TLS connect timers) — same fix as BUG-064, same commit |
| BUG-077 | Sev3 | io/esp_idf.rs | `EspIdfTlsConnector::with_connect_timeout(Duration::ZERO)` failed immediately | 2026-07-11 | 2026-07-11 | src/io/esp_idf.rs:716-721 — the elapsed check is now skipped entirely when `connect_timeout.is_zero()`, matching `set_command_timeout`'s "0 disables" convention and BUG-007's precedent; verified via `scripts/check-esp-idf.sh esp32c6` |
| BUG-066 | Sev3 | quirks/mod.rs | Default `z_max()` hardcoded `256.0` instead of a named const | 2026-07-11 | 2026-07-11 | src/quirks/mod.rs:270-272 — extracted to `pub(crate) const DEFAULT_Z_MAX_MM`, matching this file's `FAN_STEP_MAX`/`FAN_ROUNDING_OFFSET` convention |
| BUG-067 | Sev3 | types/telemetry/tests.rs | `test_progress_field_removed` was a verbatim duplicate asserting nothing about `progress` | 2026-07-11 | 2026-07-11 | src/types/telemetry/tests.rs:1531-1539 — rewritten to include a stray `"progress"` key in the input JSON and assert it's silently ignored on deserialize, instead of duplicating `test_mc_percent_deserialization` |
| BUG-069 | Sev3 | ams/mapping.rs | `MaterialSource::StandardAms`/`AmsHt` fields unvalidated in `flat_channel_id()`/`to_mapping2_entry()` | 2026-07-11 | 2026-07-11 | src/ams/mapping.rs:49-105 — both methods now range-validate `ams_id`/`slot_id`, falling back to the `-1`/unmapped sentinel on an out-of-range value instead of producing a bogus channel/entry; `MaterialSource`'s fields are public so a caller can hand-build an invalid one |
| BUG-070 | Sev3 | ams/mapping.rs | `build_ams_mapping`/`build_ams_mapping2` silently dropped `filament_id == 0` | 2026-07-11 | 2026-07-11 | src/ams/mapping.rs:174-186,207-216 — both now `log::warn!` when an allocation's `filament_id` falls outside the documented `1..=N` range, instead of silently leaving that project slot unmapped with no operator-visible signal |
| BUG-071 | Sev3 | bin/bambino-cli/storage.rs | Every `FilesAction` arm bypassed `client.disconnect()` on error | 2026-07-11 | 2026-07-11 | src/bin/bambino-cli/storage.rs:97-173 — `run()`'s dispatch now runs inside an inner `async` block whose `Result` is captured; `client.disconnect()` runs unconditionally before propagating that result, instead of every arm's `?` skipping it and FTPS's graceful `QUIT` on any failure path |
| BUG-072 | Sev3 | client/connect.rs | `from_mqtt()`'s empty `ip`/`access_code` reachable via later `.with_ftps()`/`.with_camera()` | 2026-07-11 | 2026-07-11 | src/client/connect.rs — both builders now `assert!` at the call site that `ip`/`access_code` are non-empty, panicking immediately with a clear message instead of failing opaquely at actual connect time; user chose a runtime guard over a full type-state refactor (`PrinterClient` already carries 11 generic params) given Sev3 scope |
| BUG-073 | Sev3 | tests/client_test.rs | `attach_camera()`/`disconnect_camera()` never exercised by any test | 2026-07-11 | 2026-07-11 | tests/camera_test.rs — added `test_attach_and_disconnect_camera`, asserting `attach_camera()` leaves an immediately-usable connected stream and `disconnect_camera()` clears `camera_connected()` |
| BUG-074 | Sev3 | tests/client_test.rs | `set_fan_speed`'s `speed_percent > 100` clamp path never tested | 2026-07-11 | 2026-07-11 | tests/client_test.rs — added `test_set_fan_speed_clamps_above_100_percent`, asserting `150%` clamps to the same `255` PWM value as `100%` |
| BUG-078 | Sev3 | mqtt/client/mod.rs | `publish_command` unconditionally rearmed the 10s write-zombie timer | 2026-07-11 | 2026-07-11 | src/mqtt/client/mod.rs:310-317 — now only arms `write_pending_secs` on the *first* unanswered command (`if self.write_pending_secs.is_none()`); previously a steady stream of new commands could reset the timer to 0 on every call, masking an earlier command's zombie state forever |
| BUG-079 | Sev3 | tests/common/mock_mqtt.rs | `read_packet` raced inside `tokio::select!` despite not being cancellation-safe | 2026-07-11 | 2026-07-11 | tests/common/mock_mqtt.rs — `run_mock_mqtt_broker` now runs `read_packet` in a dedicated task forwarding complete packets through an `mpsc` channel, instead of racing the raw (non-cancellation-safe) multi-await read directly against `inject_rx.recv()`; a losing race previously discarded already-consumed header/length bytes, desyncing later parses |
| BUG-080 | Sev3 | mqtt/commands/print_job.rs | `subtask_id` clamped via ad hoc `clamp_task_id()` instead of `ClampedTaskId` | 2026-07-11 | 2026-07-11 | src/mqtt/commands/print_job.rs:245 — now uses `ClampedTaskId::from(config.raw_subtask_id)`, matching `sequence_id`'s type-safe clamping in the same constructor; leftover `clamp_task_id` import and doc reference removed |
| BUG-081 | Sev3 | types/telemetry/tests.rs | No test verified `bed_temperatures()` ignores `device.bed_temp` | 2026-07-11 | 2026-07-12 | src/types/telemetry/tests.rs — added `test_bed_temperatures_ignores_device_bed_temp`, setting `device.bed_temp` to a value that would decode differently if consulted and confirming the result still comes from `device.bed.info.temp` |
| BUG-082 | Sev3 | types/telemetry/tests.rs | No test exercised a composite-packed (`>500`) `chamber_temper` value through `unpack_temperature()` | 2026-07-11 | 2026-07-12 | src/types/telemetry/tests.rs — added `test_chamber_temper_composite_packed_via_unpack_temperature`, deserializing a real `print.chamber_temper` composite value and confirming correct actual/target decode |
| BUG-083 | Sev2 | ams/parser.rs | `clean_stale_tray_data`'s `is_type_cleared` defaulted to `true` on absent `tray_type`, wiping a loaded (state 11) tray's data on incremental updates that simply omit `tray_type` | 2026-07-12 | 2026-07-12 | confirmed against `reference/05_materials_ams.md`'s Bambuddy cross-check (`on_ams_change`), `unwrap_or(true)` → `unwrap_or(false)` — fixed in `18727ce` |
| BUG-084 | Sev3 | discovery/parser.rs | `parse_location`'s port parsing silently coerced an unparseable port string to `80`, indistinguishable from "no port specified" | 2026-07-12 | 2026-07-12 | unparseable port now rejects the packet (`?`) instead of defaulting — fixed in `adcbce0` |
| BUG-085 | Sev3 | discovery/parser.rs | `is_response`'s status-line check only recognized exact-case `"HTTP/"`/`"http/"`, inconsistent with this file's otherwise-thorough case-insensitive handling | 2026-07-12 | 2026-07-12 | classification made fully case-insensitive via `eq_ignore_ascii_case` — `httparse::Response::parse` itself still requires exact-case, so real-world behavior is unchanged (no firmware observed emitting non-canonical case) — fixed in `adcbce0` |
| BUG-086 | Sev3 | discovery/parser.rs | `httparse::parse(buf).ok()?` discarded the `Status::Partial` vs `Status::Complete` distinction, so a truncated SSDP packet could still parse successfully | 2026-07-12 | 2026-07-12 | now rejects unless `Status::Complete` — fixed in `adcbce0` |
| BUG-087 | Sev3 | ftps/protocol.rs | Control-char boundary bytes (`0x20`/`0x7F`) and repeated raw reply-code offsets (`3`/`4`) weren't extracted to named consts, unlike the rest of this file's literals | 2026-07-12 | 2026-07-12 | extracted `FTP_REPLY_CODE_LEN`/`FTP_REPLY_SEPARATOR_OFFSET`/`FTP_REPLY_TEXT_OFFSET`/`FTP_PATH_CONTROL_CHAR_MAX`/`FTP_PATH_DEL_CHAR`, pure refactor — fixed in `a117d4d` |
| BUG-088 | Sev2 | ftps/parser.rs | `parse_unix_listing` collapsed multi-space filenames to single spaces via `.join(" ")`, desyncing `FtpFile::name` from the printer's actual on-disk name | 2026-07-12 | 2026-07-12 | confirmed on real P1S hardware — uploading a 3-space filename then listing showed 1 space, and `delete_file` silently no-op'd (masked by its idempotent 550 handling) when called with the reported name. Fixed by slicing the untouched line remainder verbatim (new `next_token` helper) instead of re-tokenizing/rejoining — fixed in `bd7cb15` |
| BUG-090 | Sev2 | bin/bambino-cli/monitor/dashboard.rs | AMS section vanishes on partial incremental push | 2026-07-12 | 2026-07-12 | `render_dashboard` did a flat `state.insert` per top-level `print` key instead of a recursive merge, so a partial push updating `ams` sub-fields without resending the array wiped it from accumulated state. Fixed via generic `deep_merge` helper applied to `print`/`device` keys. |
| BUG-092 | Sev2 | bin/bambino-cli/monitor/mod.rs | `dump --follow` drops connection after ~30s, no keep-alive ping | 2026-07-12 | 2026-07-12 | Follow loop only called `poll_raw()`, never `send_ping()`; `MQTT_KEEP_ALIVE_SECS` (30) caused broker disconnect once idle that long. Fixed by racing `poll_raw()` against a 15s ping tick, matching `monitor::run`. |
| BUG-091 | Sev2 | client/telemetry.rs, types/telemetry/ams.rs | `update_ams_cache` wiped cached AMS array on a partial `print.ams` push | 2026-07-12 | 2026-07-12 | Confirmed via real P1S capture: `print.ams` arrives as `{"tray_tar":"3"}`-only pushes, array/fields absent. `update_ams_cache` replaced the cache wholesale on any `Some(_)`, losing the prior array. Fixed via `AmsStatusReport::merge_from`, field-by-field. |
| BUG-093 | Sev2 | client/telemetry.rs, types/telemetry/device.rs | `update_telemetry_cache` replaced cached `DeviceTelemetry` wholesale, same shape as BUG-091 one struct up | 2026-07-12 | 2026-07-12 | `self.cache.last_device = Some(device.clone())` unconditionally on any `device` push wiped `nozzle`/`extruder`/`airduct`/`bed`/`ext_tool` back to `None` when a push touched only one sub-object. Fixed via `DeviceTelemetry::merge_from`, same pattern as BUG-091; recursion into the three collection types closed separately by BUG-094. |
| BUG-094 | Sev2 | types/telemetry/device.rs | `DeviceTelemetry::merge_from` didn't recurse into `NozzleCollection`/`ExtruderCollection`/`AirductCollection`, same gap BUG-093 explicitly left open | 2026-07-12 | 2026-07-12 | Confirmed against `pybambu`/`bambuddy`, both preserve the previous value when `nozzle.info`/`extruder.info`/`airduct` fields are absent rather than clearing. Added `merge_from` on all three collections; `ctc`/`bed`/`ext_tool` left as-is (no evidence at that grain). |
| BUG-099 | Sev1 | client/types.rs, bin/bambino-cli/probe.rs | `PrintStatus::from_gcode_state` had no arm for `"PREPARE"`, so `probe.rs`'s safety-gate `printer_is_busy()` didn't recognize a printer mid-homing/bed-leveling as busy | 2026-07-12 | 2026-07-12 | Confirmed via real P1S capture (`P1S_print_sequence.ndjson`). `PREPARE` fell through to `Unknown`, so `home_axes()` wasn't refused during in-progress homing. Fixed via new `PrintStatus::Preparing`. |
| BUG-096 | Sev2 | types/telemetry/diagnostics.rs, types/telemetry/device.rs | `CtcTelemetry.info`/`.state` sibling-independence, resolved | 2026-07-12 | 2026-07-12 | Confirmed against BambuStudio's `DevChamber::ParseChamberV2_0` — `ctc.state` reads unconditionally once `ctc` is present, but `ctc.info` is gated behind its own `.contains()` check. Added `CtcTelemetry::merge_from`; `DeviceTelemetry::merge_from` now recurses into `ctc` instead of replacing it wholesale. |
| BUG-097 | Sev3 | types/telemetry/device.rs | `ExtToolTelemetry`'s own fields, resolved | 2026-07-12 | 2026-07-12 | Confirmed for `mount_3d`/`calib`/`type` against BambuStudio's `DevExtensionToolParser::ParseV2_0` (preserve-on-absence). `mount`/`low_prec`/`th_temp` unmodeled by BambuStudio, extended anyway for `merge_from` consistency (BUG-094 precedent). Added `ExtToolTelemetry::merge_from`. |
| BUG-098 | Sev2 | types/telemetry/ams.rs | `AmsUnit` per-unit field/array merge, resolved | 2026-07-12 | 2026-07-12 | Confirmed against BambuStudio's `DevFilaSystem.cpp` `ParseAmsInfo` (~L590-720) — preserve-on-absence, same wire-economy principle as BUG-091 one level deeper. Added `AmsUnit::merge_from`; `AmsStatusReport::merge_from`'s `ams` is now a keyed per-unit merge, not wholesale replace. |
| BUG-095 | Sev2 | types/telemetry/device.rs | `BedTelemetry.info`/`.state` sibling-independence, resolved | 2026-07-12 | 2026-07-12 | Confirmed via BambuStudio's `json_diff::restore_objects` (generic recursive reconstruction wired into `parse_json` for any `print.msg==1` message, confirmed live via `P1S_print_sequence.ndjson`) — `device.bed.info`/`.state` each survive a partial push independently. Added `BedTelemetry::merge_from`; `DeviceTelemetry::merge_from` now recurses into `bed`. |
| BUG-101 | Sev2 | types/telemetry/diagnostics.rs | `CtcInfo.temp`/`.target` sibling-independence, resolved | 2026-07-12 | 2026-07-12 | `CtcTelemetry::merge_from` (BUG-096) cloned `self.info` wholesale, dropping a cached `target` on a push whose `ctc.info` repeats only `temp`. `bambuddy` confirms `target` arrives independently on the wire. Added `CtcInfo::merge_from`; `CtcTelemetry::merge_from` now recurses into `info`. |
| BUG-102 | Sev2 | types/telemetry/ams.rs | `AmsTray` array-within-a-unit wasn't keyed/pruned to match BambuStudio, and `AmsUnit.tray` couldn't distinguish "key absent" from "key present as `[]`" | 2026-07-12 | 2026-07-12 | Confirmed against BambuStudio's `ParseAmsInfo`/`ParseAmsTrayInfo` (~L715-848): `tray` is now `Option<Vec<AmsTray>>` (`None`=absent, `Some(vec![])`=present-empty) and `AmsUnit::merge_from` keyed-merges + prunes trays absent from a present array. Added `AmsTray::merge_from`, deliberately not replicating BambuStudio's reset-to-default on `tag_uid`/`tray_uuid`/`remain` — see its doc comment. |
| BUG-103 | Sev3 | ams/parser.rs, client/telemetry.rs | `clean_stale_tray_data`/`evaluate_spool_presence` tested but never called from any bambino pipeline path — `client.ams()` returned the raw cache untouched | 2026-07-12 | 2026-07-12 | BambuStudio's own `DevAmsTray::reset()` equivalent is also dead code — its shipped UI gates reads on `is_exists`/`is_tray_info_ready()` instead of scrubbing. Added `PrinterClient::sanitized_ams()` (mirrors `hms()`/`active_hms_alerts()`'s raw+opt-in-decoded split); `evaluate_spool_presence` left standalone. |
| BUG-143 | Sev2 | client/ams.rs, quirks/mod.rs, quirks/models/p1.rs | `start_drying()` dispatched `ams_filament_drying` on P1P/P1S with no guard; firmware acks `result: success` and silently discards it (screen-only control) | 2026-07-16 | 2026-07-16 | Confirmed against real P1S hardware (`dry_status` stayed `0` after an acked command) and independently against a running ha-bambulab instance; corroborated by bambuddy `fix(drying)` #2533 and Bambu's own P1 manual. Added `ModelQuirks::supports_ams_remote_drying()` (false on P1); `start_drying()` now returns `ModelMismatch` before dispatch — commit `1edb858` |

## Wontfix

| ID | Sev | Module | Title | Found | Closed | Detail |
|---|---|---|---|---|---|---|
| BUG-051 | N/A | io/esp_idf.rs | `EspIdfTimer` allocated fresh (own `EspTimerService`) per dial/handshake phase instead of shared across a connect sequence | 2026-07-11 | 2026-07-11 | **Not a bug.** `esp32-hw-probe/` stress test (10,000 sequential dial+handshake `EspTimerService::new()`/drop cycles): 0 failures across ESP32-C6 and ESP32-C3, 2 runs each. `esp_timer` slot cap isn't exhausted in practice; no `bambino` code change warranted. |
| BUG-199 | N/A | error.rs, models.rs | Neither `Error` nor `BambuModel` marked `#[non_exhaustive]` despite anticipating future variants | 2026-07-16 | 2026-07-16 | Defensible pre-1.0 choice — avoids forcing `_ =>` arms on the crate's own downstream `cli` code before external consumers exist. `BambuModel::Unknown` already gives a soft escape hatch. Revisit before first external release. |
| BUG-200 | N/A | client/ams.rs | `get_version()`/`get_k_profiles()` use `get_` prefix on async network round-trip methods | 2026-07-16 | 2026-07-16 | Rust API Guidelines' `get_` caution targets trivial field accessors, not multi-step network commands; reviewing agent's own confidence was low that this is wrong for this crate's style. Not applying. |
| BUG-201 | N/A | ftps/protocol.rs | `write_command` builds CRLF payload via `String::from`+`push_str` instead of one `format!` | 2026-07-16 | 2026-07-16 | Purely stylistic, zero functional difference — out of this sweep's own scope note excluding single subjective-preference calls. |
| BUG-202 | N/A | mqtt/commands/control.rs, mqtt/commands/ams.rs | Stringly-typed command/param args (`&str`) instead of small closed-set enums | 2026-07-16 | 2026-07-16 | Reviewing agent's own assessment: "purely optional API-guidelines polish, not a correctness bug" — call sites are internally controlled today, not externally attacker-facing. Not applying. |
| BUG-203 | N/A | types/telemetry/ams.rs | Three `merge_from` impls repeat `if incoming.field.is_some() { ... }` ~40 times instead of a shared combinator | 2026-07-16 | 2026-07-16 | Reviewing agent explicitly recommended against changing this — the per-field doc comments justifying divergent merge behavior (some fields preserve-on-absence, others reset) are more valuable than DRYing it out and collapsing them risks hiding the documented exceptions. |
| BUG-204 | N/A | types/telemetry/device.rs | `fire_ext: Option<serde_json::Value>` is untyped, unlike every sibling `DeviceTelemetry` field | 2026-07-16 | 2026-07-16 | Doc comment ("H2D Pro, H2S") indicates the wire shape is only partially reverse-engineered — reads as an intentional placeholder, not an idiom lapse. Revisit once the field's full shape is confirmed against real hardware/BambuStudio. |
| BUG-089 | N/A | ftps/client.rs | `list_directory` rejects `426` unconditionally, unlike `upload_file`/`download_file` | 2026-07-12 | 2026-07-12 | **Not a bug.** 450 real P1S `LIST` calls (quiet/concurrent-print/throttled-to-256kbps) produced zero `426`s; TLS 1.2 (already enforced) is the actual mitigation for the documented race. Even if it occurred, `LIST` has no `SIZE`-equivalent recheck, so tolerating `426` would be wrong — full retry (the caller's existing fallback) is the correct recovery |
| BUG-100 | N/A | client/telemetry.rs, types/telemetry/ams.rs | `VirtualTray` (`vt_tray`/`vir_slot`) replaced wholesale on presence, no per-field merge | 2026-07-12 | 2026-07-12 | **Not a bug.** BambuStudio's own `parse_vt_tray` rebuilds `DevAmsTray` from scratch each push (reset-on-absence) — confirmed by BambuStudio + bambuddy both; pybambu's defensive merge isn't wire-behavior evidence. Current bambino wholesale-replace is correct as-is. |
| BUG-113 | N/A | types/telemetry/device.rs, types/telemetry/mod.rs | Bed-temp value-source disagreement re-closed: `device.bed_temp`/`device.bed.info.temp` confirmed genuinely redundant, re-confirms BUG-054/081 | 2026-07-12 | 2026-07-12 | **Not a bug.** pybambu commit `6939db2` (PR #1282) has a real captured payload with both fields carrying identical values simultaneously (duplicate encoding, not competing sources); BambuStudio's `DevBed.cpp` has no fallback path reading the nested field, confirming it. `decode_bed_temperatures()`'s existing preference is correct as-is. |
| BUG-115 | N/A | ams/parser.rs | AMS-HT bit-offset collision risk closed: real hardware never exceeds 4 standard AMS units, base-16 offset can't collide | 2026-07-12 | 2026-07-12 | **Not a bug.** Official Bambu Lab documentation confirms every product line caps standard (non-HT) AMS units at 4, well clear of BambuStudio's fixed base-16 AMS-HT offset. Same evidence reopened BUG-068 as BUG-125 (Open) — see that row. |
| BUG-141 | N/A | bin/bambino-cli/monitor/mod.rs | Ping-failure handling asymmetry between `dump()` and `run()` | 2026-07-15 | 2026-07-15 | **Not a bug.** `dump()` propagates a failed `send_ping()` as fatal (`?`); `run()` logs and continues. `dump()`'s fail-fast is arguably the safer of the two (exits loudly instead of risking a silent hang) and the two functions have no stated contract requiring identical ping-failure handling. |
| BUG-142 | N/A | ftps/parser.rs | UNIX `LIST` symlink entries not special-cased | 2026-07-15 | 2026-07-15 | **Not a bug.** `perms.starts_with('l')` isn't checked, so a symlink's `" -> target"` suffix folds into `FtpFile.name` verbatim. Bambu's embedded SD card/vsftpd setup has no realistic path to producing a symlink entry — no concrete failure scenario against real hardware, only a theoretical gap against input this device class never produces. |
| BUG-162 | N/A | client/telemetry.rs | `poll_raw()` has no dedicated test | 2026-07-16 | 2026-07-16 | **Not a bug.** `poll_raw()` is a one-line delegate (`ensure_mqtt().await?` + `poll_telemetry_with_timer()`) with no independent branching logic — adequately covered transitively by `poll_telemetry()`'s existing tests, which exercise the identical underlying call. |
