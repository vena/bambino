# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

.DS_Store
.git
.env
env/
*.xml
/.embuild
*.log
*.so
/certs
*.der
.lean-ctx/
/reference/specs
/reference/probes
/review
probe_report*.json
# ack-probe reports carry the printer serial in a `serial` field — same handling as above.
ack_probe_report*.json

# Local MQTT field-mapping scratch notes, not a reference doc.
/MQTT_FIELDS.md

# libreoffice lock files
.~lock.*.csv#

# Ad-hoc telemetry captures. A raw pushall payload carries the printer serial (and the
# `ams`/`net` blocks carry more), so these are treated the same as the probe reports above:
# useful locally for checking a wire shape against what the crate binds, never committed.
test.json
/captures/